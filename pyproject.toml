[project]
name = "yob_auth"
authors = [{name = "Shayona Technology", email = "support@example.com"}]
description = "Central authentication and application access layer for first-party YOB Frappe apps"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core>=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0,<17.0.0"
