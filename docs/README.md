# YOB Auth App Documentation

Keep auth-only implementation facts, endpoint inventories, DocType details,
migrations, and security evidence here. Shared platform rules remain under
`yob_core/docs/platform/`.

The Auth app owns a restricted Apps Page entry and restricted primary Workspace
because approved administrators manage its main/Single DocTypes through Desk.
